function initialiseballs() {               // initialise the ball positions

  ballOnePositionX = width/2           // set X coord
  ballOnePositionY = 100               // set Y coord
 
  ballOneSpeedX = 5                    // ball One speed and direction in X  
}

